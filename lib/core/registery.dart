import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';

class Registery {
  static initializeBlocs({required BuildContext context}) => [
        // BlocProvider.of(context),
      ];
}
